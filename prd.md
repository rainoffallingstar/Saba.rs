# Ryusei (流星) / Sabaki 前端重构与设计规范 (PRD)

> **定位**：面向专业棋手、围棋爱好者与 AI 研究者的现代化、原生质感围棋工作台与 SGF 树状编辑器（基于 Apple 现代设计系统规范）。

---

## 1. 项目背景与审计结论 (Audit Findings)

### 1.1 传统 Sabaki 与围棋软件痛点
1. **性能与渲染开销**：原版 Electron 架构在加载超长对局（300+ 手）或庞大分支变着树时，DOM 节点量激增易引发卡顿，棋盘高帧率交互响应延迟。
2. **AI 分析链路体验割裂**：GTP/KataGo 引擎接入复杂，缺乏直观的落子质量评价体系（如 KaTrain 风格的着法优劣色环、前三候选手一览与主变 PV 动态悬浮推演）。
3. **多维信息布局拥挤**：棋盘、胜率折线图、变着树、节点属性与注释常相互挤压，导致核心棋盘视口比例失调。
4. **工作流模式缺乏统一抽象**：对弈（Match）、打谱（Record）、观战（Live）三类场景的工具栏与状态反馈未作清晰解耦。

### 1.2 重构设计目标
- **极致专注与克制美学**：遵循 Apple 设计系统（SF Pro 文字体系、纯白/深空黑二元节奏、苹果蓝 `#0071e3` 动作点睛、980px 胶囊几何），打造无干扰的沉浸式对局与研讨体验。
- **三栏 + 底部可伸缩架构**：棋盘始终保持视觉中心地位，周围面板（引擎控制、候选走法、胜率图、分支树、研讨笔记）按需智能平铺与收折。
- **全链路 AI 深度融合**：从实时单点推演、全谱批量复盘、双方目数波动诊断到拟人段位（HumanSL）对弈，提供极速响应的视觉与交互反馈。
- **现代化 SGF 树状研讨**：全功能分支树可视化、Markdown 注释渲染、丰富盘面标记工具与多格式（SGF/图片/动图/打印）导出。

---

## 2. 用户角色与核心场景 (User Personas & Jobs)

| 角色 | 典型场景 (Jobs to be done) | 关键需求 |
|---|---|---|
| **业余进阶/冲段棋手** | 自主复盘对局、查阅定式与死活分支、寻找大恶手 | 毫秒级胜率曲线、一键恶手定位、PV 变着动态预览、双方亏损目数统计 |
| **职业棋手/冲段道场教练** | 编写专题棋谱、录制教学解说、标注参考图 | 树状多分支任意穿梭、富文本/Markdown 研讨注释、多类型盘面标记（三角/圆圈/字母/死活死子标记） |
| **AI 爱好者与研究员** | 调试 KataGo/GTP 引擎权重、多模型对弈评测、配置古棋还棋头规则 | GTP 终端实时监控、多访问量（Visits）模式切换、座子制与中国古棋规则配置 |
| **日常对弈与观战者** | 在线与 AI/真人对局、计时读秒、导入野狐/奕城/OGS 棋谱 | 极简时钟面板、读秒语音反馈、多标签页快速切换与云端棋谱同步 |

---

## 3. 信息架构与界面布局系统 (Information Architecture)

```
┌────────────────────────────────────────────────────────────────────────────────────────┐
│ 1. 顶部全局导航栏 (Titlebar & Global Header)                                           │
│   [Traffic Lights] [侧栏开关] [对局标题与状态] [黑白对阵胶囊 / 时钟] [模式切换] [操作区]  │
├────┬─────────────────┬───────────────────────────────────────────┬─────────────────────┤
│ 2. │ 3. 辅助侧栏     │ 4. 核心工作区 (Center Canvas)             │ 5. 右侧检视面板     │
│ 导 │ (Left Sidebar)  │   ┌───────────────────────────────────┐   │ (Right Inspector)   │
│ 航 │                 │   │ 浮动盘面工具条 (Markup / Tools)   │   │                     │
│ 轨 │ • 引擎状态/切换 │   │ ┌───────────────────────────────┐ │   │ • AI 候选手排序榜   │
│ (N │ • 棋谱库/多会话 │   │ │                               │ │   │   (Winrate/Score/PV)│
│ a  │ • 规则与时钟    │   │ │      19×19 高清棋盘 (Goban)   │ │   │ • 当前节点属性      │
│ v) │ • 历史对局记录  │   │ │                               │ │   │ • Markdown 研讨注释 │
│    │                 │   │ └───────────────────────────────┘ │   │ • 对局基本信息表单  │
│    │                 │   │ 底部悬浮控制条 (Nav / Playback)   │   │                     │
│    │                 │   └───────────────────────────────────┘   │                     │
│    │                 ├───────────────────────────────────────────┤                     │
│    │                 │ 6. 底部多功能工作台 (Bottom Analysis Deck)│                     │
│    │                 │   [ 胜率/目数差走势图 | 横向变着树 | GTP 控制台 | 插件扩展 ]    │
└────┴─────────────────┴───────────────────────────────────────────┴─────────────────────┘
```

---

## 4. 核心功能模块与交互规范 (Core Modules & Interactions)

### 4.1 核心棋盘视口 (Goban Viewport)
- **视觉风格**：
  - 支持 **新榧木纹 (Kaya Wood)**、**极简白墨 (Studio Light)**、**深空曜黑 (Midnight Dark)** 三套主题。
  - 拟真微阴影与贝壳/花岗岩质感棋子渲染，黑白棋子具有微高光区分。
- **AI 动态叠加层 (AI Overlays)**：
  - **推荐点圆点 (Candidate Dots)**：以胜率最高点为金边蓝点，其余候选手根据胜率阶梯呈绿色/橙色半透明光环，内嵌访问量与目数差标签。
  - **主变预览 (PV Sequence Ghosting)**：鼠标悬停任意候选手，盘面自动半透明投射出该分支后续 3~10 手推演（带序号与黑白交替虚影）。
  - **落子优劣评价 (Move Quality Badges)**：
    - `Best` (最优手，蓝色光环)
    - `Good` (好手，绿色光环)
    - `Inaccuracy` (缓手，黄色)
    - `Mistake` (失误，橙色)
    - `Blunder` (大恶手，红色闪烁微标)
- **盘面标记工具箱 (Floating Markup Bar)**：
  - 圈选（Circle）、三角（Triangle）、方块（Square）、十字（Cross）、字母序号（A-Z / 1-9）、死活标记、地盘估算（Territory Shading）。

### 4.2 底部分析甲板 (Bottom Analysis Deck)
- **Tab 1: 胜率与目数差走势图 (Winrate & Score Graph)**
  - OGS 风格双向胜率填充（黑色区域向上增长，白色区域向下增长，中轴 50% 基准线）。
  - 支持切换【胜率曲线 Winrate %】与【目数领先 Score Lead ±pts】双轨模式。
  - 底部手数标尺（1 ~ 300+ 手）附带失误点预警条（红黄竖线），点击任意拐点瞬时跳转至对应局面。
- **Tab 2: 横向分支变着树 (Horizontal Variation Tree)**
  - 现代化树状节点图，主干居中加粗，分支分层平滑连线。
  - 节点根据 AI 评级显示色彩编码，支持折叠未探索分支、设为主干、删除分支。
- **Tab 3: GTP 终端与引擎监控 (GTP Terminal & Diagnostics)**
  - 查看 KataGo 实时推演日志、NNCache 命中率、GPU 推理耗时、输入原生 GTP 指令。

### 4.3 右侧智能检视器 (Right Inspector)
- **Top 区域：AI 候选走法列表 (Candidate Move Ranker)**
  - 卡片式列出 Top 5 候选着法（坐标、胜率 %、目数领先 ±pts、访问量 Visits、先验概率 Prior %）。
  - 支持一键推演（Play out）、一键加入变着树（Add as Variation）。
- **Middle 区域：节点信息与研讨注释 (Node Notes & Markdown)**
  - 支持 Markdown 语法实时预览、代码块、定式名称标签、局面形势简评。
- **Bottom 区域：整局复盘统计 (Game Loss Summary)**
  - 双方恶手数/失误数对比条形图、每手平均损失目数（Average Loss per Move）。

### 4.4 会话与对局模式 (Session Modes)
1. **打谱研讨 (Record Mode)**：自由摆子、变着推演、编辑 SGF 属性、批量批注。
2. **AI 对弈 (Match Mode)**：支持按段位配置 KataGo HumanSL（20K ~ 9D），配置用时规则（日本读秒 / 传统包干 / 读秒加秒制）。
3. **棋谱库同步 (Library & Fox Sync)**：支持本地 SGF 归档管理与一键拉取野狐围棋等网络对局。

---

## 5. 设计系统与视觉规范 (Apple Design System Tokens)

本方案严格绑定 Apple 设计系统核心令牌：

| Token | 值 / 规则 | 应用场景 |
|---|---|---|
| `--bg` | `#ffffff` / `#000000` (暗色) | 主画布底色 |
| `--surface` | `#f5f5f7` | 侧栏、面板卡片、折线图背景底板 |
| `--surface-warm` | `#fbfbfd` | 棋盘衬底、高亮面板容器 |
| `--fg` | `#1d1d1f` | 一级标题、主要文字、黑子标号 |
| `--fg-2` | `#424245` | 次级文字、快捷键提示、数据标签 |
| `--muted` | `#6e6e73` | 辅助说明、SGF 属性标签、禁用项 |
| `--border` | `#d2d2d7` | 分割线、输入框边框、面板边缘 |
| `--accent` | `#0071e3` (Apple Blue) | 主行动按钮、当前选中手、最高胜率着法高亮 |
| `--accent-hover` | `#0077ed` | 交互悬停提亮态 |
| `--radius-sm/md/lg` | `8px` / `12px` / `18px` | 按钮、输入框、卡片圆角 |
| `--radius-pill` | `980px` | 苹果胶囊按钮、胜率标签、状态指示器 |
| `--font-display` | `SF Pro Display, -apple-system` | 标题、胜率大字展示 |
| `--font-body` | `SF Pro Text, -apple-system` | 界面正文、棋谱注释、参数选项 |
| `--font-mono` | `SF Mono, JetBrains Mono` | 坐标轴刻度、GTP 日志、SGF 源码 |

---

## 6. 前端实现架构与原型交付计划

### 6.1 原型交付范围 (High-Fidelity Web Prototype)
生成一个完整自包含的现代化前端单页原型 `sabaki-web-prototype.html`，涵盖：
1. **全尺寸高保真 19×19 围棋棋盘交互**：支持真实下子、提子判断、悬停高亮、试下推演与撤销。
2. **完整 KataGo AI 模拟推演引擎**：
   - 实时计算候选点光环与数值标签。
   - 悬浮在候选点上展示虚影 PV 走法连线。
3. **OGS/KaTrain 级底部多功能面板**：
   - 可交互的胜率/目数折线图（点击任意手跳转盘面）。
   - 实时生成的分支变着树（支持多分支切换）。
   - 实时 GTP 控制台模拟终端。
4. **右侧智能复盘面板**：
   - 候选走法列表（按胜率/目数排序，可交互推演）。
   - 双方恶手分析与胜率亏损榜单。
   - 实时 Markdown 棋谱注释编辑器。
5. **完整的主题与模式切换**：
   - 棋盘材质切换（榧木纹理 / 极简白墨 / 深空黑夜）。
   - 会话模式切换（打谱 Record / 对弈 Match / 棋谱库 Library）。

---

## 7. 开放讨论问题 (Open Questions & TODOs)

- [ ] **Q1 (棋盘材质)**：是否需要支持用户自定义木纹材质与真实落子音效（石子碰盘声）？
- [ ] **Q2 (AI 引擎策略)**：默认展示 Top 3 还是 Top 5 候选点？是否需要默认开启落子失误警告弹窗？
- [ ] **Q3 (古棋规则)**：是否在对弈设置中默认提供「中国古棋还棋头 + 四角座子」专属预设？

---

## 8. 下一步操作 (Next Steps)

请审阅当前规划文档 `prd.md`：
1. 如需调整布局结构、功能重点或视觉风格，可直接回复修改意见或编辑此文件。
2. 确认无误后，我们将正式进入 **Design Mode**，构建并交付高保真交互式原型 `sabaki-web-prototype.html`。
