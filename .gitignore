/target/
/dist/

# Archived upstream Electron/Tauri reference repository. It keeps its own Git
# history locally and is intentionally excluded from the Rust/GPUI mainline.
/refer-repo/

# Editor / OS noise
.DS_Store
*.swp

# Engine integration test cache (downloaded nets, gtp configs)
.engine-cache/

# GTP communication logs written at runtime when engines are attached
gtp_logs/

# Example plugin build artifacts
/examples/plugins/*/org.ryusei.sgf-exporter

# KataGo runtime logs created when tests spawn the real engine
katago_logs/
